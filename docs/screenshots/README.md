# Implementation screenshots

> **Current screenshot proof — July 19, 2026:** the eight JPGs and `screenshot-receipt.json` under `docs/screenshots/reasonweave-final-july19-r1/` are the authoritative current set. The receipt binds every byte, path, exact clean source SHA, clean-before-capture assertion, and canonical ReasonWeave identity; its recorded hashes must match the tracked files. Full-size visual inspection and independent review found no P0/P1: the creation-anchor helper is separated from both controls, the mobile outline shows all three branch questions and the learner-selected question, and seeded/demo provenance remains visible. The route overview is supporting context rather than stand-alone completion proof because its card bottoms extend below the 720-pixel frame. No live OpenAI, provider, video, deployment, publication, or human approval is claimed.

> **Underlying application proof — July 19, 2026:** `0bd5aef89ab4715477491b462c13be0c5978ac6b` (tree `4d6162e7a7632bba96f582818f44d98bbff956a0`) passed direct clean committed-worktree format/lint/typecheck; 46/596 Vitest; 145/145 fixtures; build; initial bundle 464,236/113,930 (map 16,356/5,592; card 15,598/5,051; UI 31,954/10,643; seeded 21,224/6,904); Playwright 28/68; no-key 1/1 zero API; keyboard stress 5/5; audits 0; expected-only scans; diff and clean status. The later screenshot source adds only release-input/capture framing and the reviewed anchor-helper spacing fix. `acfdc37` is historical predecessor proof.

**Historical-ledger convention:** every later `acfdc37` reference preserves predecessor proof only; it is never current screenshot or application proof.

The same `0bd5aef` source also passed a fresh no-hardlink/no-alternates clone (exact HEAD/tree, unique pack inode/link count 1, and `npm ci` adding 464 packages); keyboard stress 5/5 remains direct-worktree proof.

## Current versioned release set

The current set preserves the same eight-stage story while adding the exact **Creation anchor phrase** and refreshed nine-node evidence-to-creation trace. Dimensions are 1280 × 720 for Spark, routes, and prediction; 751 × 481 for the focused evidence-to-design panel; 1440 × 810 for the map-first judge hero; 840 × 2189 for the complete Discovery Card; and 375 × 812 for both mobile frames. `discovery-mobile-trace.jpg` deliberately shows the two suggested branches followed by the learner-selected **My next question**; `evidence-create-desktop.jpg` visibly carries **regular deliveries** from the design move into the anchor field.

These screenshots prove the deterministic pre-generated journey rendered at the exact clean source recorded in their receipt; they do not prove a credentialed model call, deployment, public upload, or human usability outcome. Current media rendering and capture remain separate gates.

## Historical screenshot ledger

These frames were captured from the locally running **ReasonWeave** implementation on July 18, 2026 at exact application checkpoint `aa64be1d200a21463e8ef187fa08e9f9800a8ffd`. They include the required Branch selection: the learner chooses exactly one of three suggested next questions before the Discovery Card/Markdown export unlock, and the chosen value is labeled **My next question**. The focused Chromium screenshot journey passed three times against the same source state, including once after the checkpoint commit; all eight JPGs regenerated byte-for-byte across those runs. This is historical screenshot evidence, not current `0bd5aef89ab4715477491b462c13be0c5978ac6b` proof; `acfdc37`, `035e820`, `9f7e53f`, and `5ddbac3` are historical predecessors. Earlier `dac76a2`, `ae3df74`, and `0e0547e` browser/screenshot evidence is also historical.

> **Name and media note — July 19, 2026:** The owner adopted **ReasonWeave**, with the promise **make reasoning visible**, despite its documented unresolved collision risk; this is not legal clearance. These JPGs are bound to clean historical application checkpoint `aa64be1`. Local narrated capture and assembly proof remains bound to older source `5814a13` and reuses immutable provider artifacts from `10a058e`, so it is historical relative to current checkpoint `0bd5aef89ab4715477491b462c13be0c5978ac6b`. None of these still frames alone proves a live-model run, deployment, final public video, or public upload.

- `spark-desktop.jpg` — 1280 × 720 desktop Spark with the canonical question filled and the prediction-before-evidence contract visible
- `prediction-desktop.jpg` — 1280 × 720 desktop prediction gate with learner text present and no evidence revealed
- `routes-desktop.jpg` — 1280 × 720 desktop route chooser with three distinct methods
- `evidence-create-desktop.jpg` — 751 × 332 focused desktop learner evidence-to-design panel
- `discovery-desktop.jpg` — 1440 × 810 map-first judge hero with the complete nine-node Curiosity Map, Replay trail, selected **My next question**, and Discovery Card
- `discovery-card-desktop.jpg` — 840 × 1536 focused tablet-width learner trace with the selected finding, relationship, establishes/unresolved/impact claims, evidence-to-design choice, and learner/demo provenance separated
- `discovery-mobile.jpg` — 375 × 812 intentional top-of-card mobile view with the title, original question, route, quest question, prediction, and start of evidence in context
- `discovery-mobile-trace.jpg` — 375 × 812 mobile Curiosity Map text outline showing Creation, Changed model, all three next questions, and the personalized **My next question** payoff without relying on SVG layout

They document the pre-generated demo state, not a live GPT-5.6 request. Together the frames show the Evidence Decision's finding, relationship, three establishes/unresolved/impact claims, and a required 20–400 character evidence-to-design choice carried into the map and Discovery Card. The choice is tied to the same selected finding and clears if that finding changes; Branch requires one selected suggested question before card/export unlock, labels it **My next question**, and does not automatically continue into another quest. Historical predecessor checkpoint `acfdc37c3bc3e13fefc24d8fdb2c82b214b38f5a` passed its authoritative serial Playwright matrix at 28 passes / 68 deliberate scoped skips, with 44/509, 145/145, initial bundle 458,996/112,371, Map 16,372/5,682, Card 15,163/4,918, UI 31,535/10,600, seeded demo 21,186/6,890, and no-key 1/1 zero API requests. This is historical automated source proof, not a screenshot refresh. Physical/manual Safari, human/manual keyboard and broader accessibility review, true 200% zoom, VoiceOver/NVDA/JAWS, signed-out/deployed review, owner/human narration approval, and public-video verification remain pending.

| File                          | SHA-256                                                            |
| ----------------------------- | ------------------------------------------------------------------ |
| `discovery-card-desktop.jpg`  | `209259270970bf753fff89d4b9dee2a4cdc43e94c493b4474a39dcac670cc08f` |
| `discovery-desktop.jpg`       | `cf84490dab650c0587578d4df18f33b6b6b8aa1571f0b84ce80b81b647d45e55` |
| `discovery-mobile-trace.jpg`  | `3eced06edc6df2393e79448c8419dbf68fe7db85799ec53378d7dba36e6d64cf` |
| `discovery-mobile.jpg`        | `3bece7c3a4e458ab63e8e223c66ea6bcf412fc1906689b84e2c09e585542d6c2` |
| `evidence-create-desktop.jpg` | `bb80dd7371806ec34096bcb66987f7b18b325280289f38b9e63556a6a5bd4a2c` |
| `prediction-desktop.jpg`      | `b03abbe64e68edbf0806138333e463e8c508cfc282e7cdba718905bd378260b8` |
| `routes-desktop.jpg`          | `9969ac29a8e9a0de8e4da6d071ea35a7173cf23ef6a1beee79634df39769eaf3` |
| `spark-desktop.jpg`           | `bfe9131722afc1422097d90611607f60d9a679b8d6cf664747db929d144360c9` |

Regenerate this historical screenshot set from its bound implementation with:

```bash
WONDERLAB_CAPTURE_SCREENSHOTS=true npm run test:e2e -- --project=chromium
```

That command is retained only as a record of the historical capture workflow. At the current source it fails closed because it omits a fresh versioned output; it cannot overwrite the eight hash-bound JPGs above.

## Versioned current captures

After the owner adopts the final public name and the renamed source plus release inputs are committed, capture into one new lowercase direct child of `docs/screenshots/`:

```bash
WONDERLAB_CAPTURE_SCREENSHOTS=true \
WONDERLAB_SCREENSHOT_OUTPUT=docs/screenshots/${PUBLIC_SLUG}-<release-id> \
  npx playwright test e2e/seeded-flow.spec.ts --project=chromium \
    --grep 'the complete seeded quest preserves the learning gate and exports a nine-node trace'
```

The checkout must be clean before the directory is created. The capture reserves exactly the eight fixed filenames, keeps each Playwright image in memory until it can write through the reserved file's inode, and rejects a directory or file replacement before publication. Finalization rechecks the Git SHA, worktree scope, directory identity, file identity, byte count, and write-time hash before exclusively creating `screenshot-receipt.json`. Existing, nested, absolute, traversal, uppercase, symlinked, or base-directory targets are rejected; reruns must use another fresh release ID.

The release-media renderer accepts `discovery-desktop.jpg` only when the matching owner marker and receipt bind all eight current screenshots to the same clean source and every recorded byte count and SHA-256 still matches. This path work makes no screenshot, provider, Keychain, or OpenAI request by itself.
